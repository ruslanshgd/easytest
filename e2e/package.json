{
  "name": "figma-test-e2e",
  "description": "E2E-тесты (Playwright) по чек-листу QA: разделы 1–3, 7, 8. Подробнее — README.md в этой папке.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:analytics": "playwright test --project=analytics",
    "test:viewer": "playwright test --project=viewer"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
