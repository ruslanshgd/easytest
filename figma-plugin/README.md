# Figma Plugin - Изи Тест

## 🚀 Быстрый старт

### Первая настройка (онбординг)

При первом запуске плагин автоматически покажет форму настройки:

1. **Запустите плагин:** Plugins → Изи Тест
2. **Заполните форму настройки:**
   - **Supabase URL:** URL вашего Supabase проекта (например: `https://xxxxx.supabase.co`)
   - **Supabase Anon Key:** Anon ключ из Supabase Dashboard (Settings → API)
   - **Viewer URL:** URL вашего viewer приложения (для локальной разработки: `http://localhost:5173`)
3. **Нажмите "Сохранить настройки"**
4. Конфигурация сохранится автоматически и будет использоваться при следующих запусках

### Где найти ключи Supabase?

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings → API**
4. Скопируйте:
   - **Project URL** → вставьте в поле "Supabase URL"
   - **anon/public key** → вставьте в поле "Supabase Anon Key"

---

## 📋 Ручная настройка (альтернатива)

Если вы предпочитаете настроить вручную, откройте `figma-plugin/code.js` и обновите объект `CONFIG`:

```javascript
const CONFIG = {
  SUPABASE_URL: "https://your-project.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-key-here",
  VIEWER_URL: "http://localhost:5173" // или ваш production URL
};
```

**Важно:** Конфигурация плагина должна совпадать с настройками в `figma-viewer/.env`:
- `VITE_SUPABASE_URL` → `CONFIG.SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY` → `CONFIG.SUPABASE_ANON_KEY`

---

## 🔐 Авторизация и безопасность

### Для респондентов (тестирование прототипов)

1. Респондент открывает ссылку на прототип
2. В первый раз потребуется **авторизация через email** (OTP код)
3. После авторизации прототип загружается и начинается тестирование
4. Все события тестирования сохраняются в Supabase с привязкой к пользователю

### Для дизайнеров (создание прототипов)

- Плагин создает прототипы **без привязки к пользователю** (публичные прототипы)
- Это позволяет создавать прототипы до авторизации в viewer
- RLS политики в Supabase разрешают создание прототипов без `user_id`

### Изменение настроек

Чтобы изменить сохраненную конфигурацию:
1. Запустите плагин
2. Нажмите кнопку "⚙️ Настройки" в интерфейсе плагина
3. Обновите значения и сохраните

---

## 🚀 Установка плагина

### Вариант 1: Локальная разработка
1. Откройте Figma Desktop
2. Plugins → Development → Import plugin from manifest...
3. Выберите `figma-plugin/manifest.json`

### Вариант 2: Публикация
1. Опубликуйте плагин в Figma Community
2. Установите через Plugins → Browse plugins

---

## 📝 Использование

1. **Создайте макеты в Figma** с прототипами и хотспотами
2. **Запустите плагин:** Plugins → Изи Тест
3. **Нажмите "Отправить на тест"** (плагин автоматически экспортирует JSON)
4. **Скопируйте ссылку** для респондента
5. **Отправьте ссылку респонденту** — он откроет её в браузере, авторизуется и начнет тестирование

### Советы по использованию

- **Стартовый экран:** Выделите нужный фрейм или добавьте `[start]` в название
- **Финальный экран:** Добавьте `[final]` в название финального фрейма для точного определения
- **Хотспоты:** Используйте Figma Prototype для создания интерактивных переходов

---

## ⚙️ Технические детали

### Архитектура

- **Конфигурация:** Сохраняется через `figma.clientStorage` (персистентное хранилище плагина)
- **Передача данных:** `postMessage` для связи между `code.js` и `ui.html`
- **Прототипы:** Создаются в Supabase без `user_id` (публичные), доступны всем авторизованным пользователям
- **Сессии:** Создаются автоматически при открытии ссылки на прототип в viewer

### Для production

При деплое viewer'а на production:
1. Обновите `VIEWER_URL` в настройках плагина на production URL
2. CORS настраивается автоматически в Supabase (не требует ручной настройки)

### Структура данных

- **Прототипы** (`prototypes`): Хранят JSON данные прототипа, могут быть без `user_id`
- **Сессии** (`sessions`): Создаются при открытии прототипа, привязаны к `user_id` через RLS
- **События** (`events`): Записываются во время тестирования, привязаны к сессии и `user_id`

